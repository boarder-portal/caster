.root {
  flex-grow: 1;
  display: grid;

  :global(body.desktop) & {
    grid-template:
      "player chat" calc(100vh - 140px)
      / 4fr 1fr;
    gap: 20px;
  }

  :global(body.mobile) & {
    grid-template:
      "player" calc(100vw / (16 / 9))
      "chat" calc(100% - 100vw / (16 / 9))
      / 100%;
    height: 100%;
  }
}

.playerContainer {
  grid-area: player;
  align-items: center;
  justify-items: center;
  width: 100%;
  height: 100%;

  .player {
    width: 100%;
    height: 100%;
  }

  :global(body.mobile) & {
    aspect-ratio: 16 / 9;
  }
}

.chat {
  grid-area: chat;
}

:global(body.desktop) {
  .player, .chat {
    overflow: hidden;
    border-radius: 4px;
    box-shadow: var(--boxShadow);
  }
}
